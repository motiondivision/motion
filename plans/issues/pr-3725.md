# Plan pr-3725: Close "Add regression test for accordion rapid-click exit animation (#2674)"

> **Executor instructions**: Follow this plan step by step. This is a close
> action with a maintainer gate. If anything in "STOP conditions" occurs, stop
> and report. When done, update this plan's row in `plans/issues/README.md`.
>
> **Drift check (run first)**: `gh pr view 3725 --json state,headRefOid,isDraft`
> Expected: `state: OPEN`, `isDraft: true`,
> `headRefOid: 9f4ed66fa8adfdbc2eb6044c725e1126cd430295`.

## Status

- **Priority**: P3
- **Effort**: S
- **Risk**: LOW
- **Depends on**: maintainer approval (gate below)
- **Category**: tests
- **Planned at**: commit `42bfbe3ed`, 2026-06-11
- **PR**: https://github.com/motiondivision/motion/pull/3725 (branch `worktree-fix-issue-2674`, DRAFT, test-only, +142/−0)
- **Linked issue**: #2674 (open since May 2024; CodeSandbox repro was
  Cloudflare-blocked when the PR was authored — the scenario is reconstructed
  from the description, not the reporter's code)

## Why this matters

The PR is a draft that itself invites this decision: "Opening as draft because
no production code is changed — please confirm whether the bug is indeed
considered resolved and the test should land on its own. If it should be
closed without merging, that's also fine." The author could not reproduce the
bug ("All three test variants pass… without any code changes") and could not
fetch the reporter's repro. Per the repo's recorded policy ("no repro → no
fix, no speculative coverage"), the default disposition is close. The
counterpoint, for the gate: the three scenarios (re-open mid-exit, close
mid-enter, 5× rapid toggle on keyed children with their own `AnimatePresence`
and `height: 0 ↔ "auto"`) are final-state assertions on a behaviorally
distinct interaction pattern not otherwise covered, and CI is fully green — if
the maintainer wants belt-and-braces AnimatePresence coverage, the merge path
is cheap.

## Current state

- 2 new files: `dev/react/src/tests/animate-presence-accordion-rapid.tsx`,
  `packages/framer-motion/cypress/integration/animate-presence-accordion-rapid.ts`.
- CI: ALL GREEN at planning time. Zero comments, zero reviews.
- The PR attributes the apparent fix to six prior AnimatePresence commits
  (`90d8c5364`, `10427ae38`, `6a8d3abb9`, `05842be0d`, `8798d7017`, `aa8b46be3`).
- Note: PR #3707 (plan pr-3707) fixes a *different* AnimatePresence stuck-exit
  bug with a real failing-first test — that's the regression gate for the
  stuck-exit class; this PR's tests never failed on anything.

## Steps

### Step 1 (GATE): maintainer decision — close (default) or merge

If the `plans/issues/README.md` row is not marked APPROVED-CLOSE or
APPROVED-MERGE, set it to BLOCKED ("awaiting close-vs-merge decision the PR
body itself requests") and stop.

### Step 2a (APPROVED-CLOSE, default): close PR, disposition issue

`gh pr close 3725 --comment "Closing per repo policy (no repro → no speculative coverage): the bug could not be reproduced against the current codebase and the reporter's sandbox was unobtainable, so these tests never failed on the reported bug. See plans/issues/pr-3725.md."`
Then comment on #2674 asking the reporter to re-test on current `motion`
(the rewrite of exit handling since v11 likely fixed it) and close it
not_planned (`gh api -X PATCH repos/motiondivision/motion/issues/2674 -f state=closed -f state_reason=not_planned`
if `gh issue close` hits the known GraphQL error).

### Step 2b (APPROVED-MERGE): land the coverage

`gh pr checkout 3725`; rebase onto `origin/main`; run the spec locally on BOTH
React 18 and 19 per the CLAUDE.md Cypress recipe; `gh pr ready 3725`; push;
green CI; `gh pr merge 3725 --squash`. Do not let the merge auto-close #2674
as "fixed by this PR" — the tests don't prove the fix; if the body says
"Fixes #2674", remove that line first via
`gh api -X PATCH repos/motiondivision/motion/pulls/3725 -f body="..."`
(NOT `gh pr edit` — known broken).

**Verify**: exactly one of Step 2a / 2b completed; PR state matches.

## Done criteria

- [ ] Gate cleared with an explicit APPROVED-CLOSE or APPROVED-MERGE
- [ ] PR closed with policy comment + issue dispositioned, OR PR merged with
      green CI on both React versions and the auto-close reference removed
- [ ] `plans/issues/README.md` row updated

## STOP conditions

- New activity on #2674 since planning (a fresh repro changes everything —
  report for re-planning).
- In the merge path, the spec fails on either React version.
