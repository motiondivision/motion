# Plan pr-3750: Land dependabot "Bump shell-quote from 1.8.3 to 1.8.4"

> **Executor instructions**: Follow this plan step by step. If anything in
> "STOP conditions" occurs, stop and report. When done, update this plan's row
> in `plans/issues/README.md`.
>
> **Drift check (run first)**: `gh pr view 3750 --json state,headRefOid`
> Expected: `state: OPEN`,
> `headRefOid: cad6f7f113376a2f276cc5134c8a4c14a6c7e4a0`. (Dependabot may have
> force-pushed a rebase — a changed head is fine HERE as long as the diff is
> still yarn.lock-only; re-check with `gh pr diff 3750 --name-only`.)

## Status

- **Priority**: P3
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: dependencies
- **Planned at**: commit `42bfbe3ed`, 2026-06-11
- **PR**: https://github.com/motiondivision/motion/pull/3750

## Why this matters

Routine patch bump of a transitive, dev-only dependency (shell-quote, pulled
in by `concurrently@7.6.0` and `react-dev-utils@12`, both root
devDependencies — nothing shipped). The only functional upstream change is
"quote: validate object-token shapes"; no security advisory. It is BLOCKED
solely because the required `ci/circleci: test-react` check failed on one
known-flaky Cypress spec (`layout-group.ts` — "relative children should not
instantly jump to new layout"), which (a) passed under React 19 in the same
run and (b) is part of a drag/layout flake family that fails on main itself
(main HEAD had `drag-svg-viewbox.ts` red at planning time). Zero-risk merge
once CI is re-run.

## Current state

- Diff: `yarn.lock` only — `shell-quote@npm:^1.7.3` resolution 1.8.3 → 1.8.4.
  No package.json changes.
- Checks at planning time: setup/test/test-html/test-react-19 pass;
  `test-react` FAIL on the flaky spec above.

## Steps

### Step 1: Trigger a fresh CI run

Comment `@dependabot rebase` on the PR
(`gh pr comment 3750 --body "@dependabot rebase"`). This force-pushes a
rebased head and re-runs all checks. Wait, then `gh pr checks 3750 --watch`.
If a single flaky drag/layout spec fails again, one more
`@dependabot rebase`; if it fails a third time on the SAME spec, that's a STOP.

**Verify**: all required checks green; diff still `yarn.lock`-only.

### Step 2: Merge

`gh pr merge 3750 --squash` (or comment `@dependabot squash and merge` to let
dependabot handle it). Do NOT use `gh pr edit`.

**Verify**: `gh pr view 3750 --json state` → `MERGED`.

## Done criteria

- [ ] CI green on the current head; diff confirmed yarn.lock-only
- [ ] PR merged
- [ ] `plans/issues/README.md` row updated

## STOP conditions

- The diff is ever more than `yarn.lock` (dependabot config changed — read it).
- The same CI spec fails on three consecutive runs (not flake — report the
  spec name and run URLs).
