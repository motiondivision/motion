# Plan pr-3742: Close "[audit] motion-dom/stats: dedupe summarise calls" as superseded by #3744

> **Executor instructions**: This plan is a close action, executed as Step 3 of
> `plans/issues/pr-3744.md`. Only execute it standalone if pr-3744 was REJECTED.
>
> **Drift check (run first)**: `gh pr view 3742 --json state,headRefOid`
> Expected: `state: OPEN`, `headRefOid: 113a25d127ae452dc843af9972aecd0b02220e5b`.

## Status

- **Priority**: P3
- **Effort**: S
- **Risk**: LOW
- **Depends on**: plans/issues/pr-3744.md (this PR closes when that one merges)
- **Category**: tech-debt
- **Planned at**: commit `42bfbe3ed`, 2026-06-11
- **PR**: https://github.com/motiondivision/motion/pull/3742 (branch `audit/motion-dom-stats`)

## Why this matters

PR #3742 refactors `packages/motion-dom/src/stats/index.ts` (dedupes 15
`summarise()` calls into a `summariseAll` helper, inlines `mean`/`msToFps`,
~120 B gz saved). Every hunk it touches is **deleted outright** by PR #3744
(~580 B gz saved from the same code). The two hard-conflict; #3744's body
explicitly says "Supersedes #3742 … Close #3742 when this lands." Keeping both
open invites a wasted-effort merge race.

## Current state

- 1 file, +20/−53, `packages/motion-dom/src/stats/index.ts` only. No public
  API change. The file is unchanged on main (verified 2026-06-11: `record()`,
  `mean`, `summarise(values, calcAverage = mean)`, `msToFps` all still
  present), so #3742 still applies cleanly — but only in a world where #3744
  does not land.
- CI at planning time: 5/6 green; `test-react-19` failed on `drag.ts`
  (known-flaky, unrelated). No human review; Greptile 5/5.

## Steps

### Primary path (pr-3744 merged)

Handled by pr-3744's Step 3:
`gh pr close 3742 --comment "Superseded by #3744..."`. Nothing else to do.

### Fallback path (only if pr-3744 was REJECTED by the maintainer)

If the maintainer rejects #3744's public-API narrowing and wants stats
reporting preserved, #3742 becomes a safe mechanical win instead:

1. `gh pr checkout 3742`; rebase onto `origin/main`; push.
2. `yarn build && npx jest --config packages/motion-dom/jest.config.json --testPathPattern="stats"` → pass.
3. Green CI (re-run once on flaky drag-spec failures), then
   `gh pr merge 3742 --squash`.

## Done criteria

- [ ] Exactly one of: #3742 closed with supersession comment (primary), or
      #3742 merged (fallback, only after #3744 rejection is recorded)
- [ ] `plans/issues/README.md` row updated

## STOP conditions

- Both #3744 and #3742 still open and #3744's decision (merge vs reject) is
  unrecorded — do not pick a winner yourself; report.
